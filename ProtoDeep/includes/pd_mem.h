#ifndef PD_MEM_H
# define PD_MEM_H

// Lib

// Macro

// Structures

// Prototypes
void            pd_mem_cpy(void* dst, void *src, size_t len);
void            pd_mem_set(void *array, int c, size_t len);

#endif